{{look-book pages=lookbook.pages page=page class="banners"}}

<section class="blog">
  <section class="posts">
    {{#each posts as |post|}}
      {{blog-post post=post}}
    {{/each}}
  </section>

  {{sticky-links instagram=instagram product=product}}
</section>
