<article class="page">
  {{#if isVideo}}
    <iframe {{bind-attr src=url}}>
  {{else}}
    <img {{bind-attr src=url}}>
  {{/if}}
</article>

{{#link-to "projects.project.page" previous class="previous"}}
  &lt;
{{/link-to}}

{{#link-to "projects.project.page" next class="next"}}
  &gt;
{{/link-to}}
